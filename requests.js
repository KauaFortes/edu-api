/*const urlAPI = 'https://kb-newdev.herokuapp.com'

async function getCourses() {
  const response = await fetch(`${urlAPI}/courses`, {
    method: 'POST',
    body: {
      test: 'valor'
    }
  })

  const dataJson = await response.json()
  console.log(dataJson)
}

getCourses()*/